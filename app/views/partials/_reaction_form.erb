<% form @next, { method: :post, action: next_path }, wrapper: :div do |f| %>
  <fieldset class="form-group margin-bottom-large reaction">
    <legend>Do you like this article?</legend>

    <input
      type="submit"
      name="reaction"
      value="-"
      class="paper-btn margin-right"
      style="background-color: var(--danger-light); font-weight: bold;"
    >

    <input
      type="submit"
      name="reaction"
      value="↻"
      class="paper-btn margin-right"
      style="font-weight: bold;"
    >

    <input
      type="submit"
      name="reaction"
      value="+"
      class="paper-btn"
      style="background-color: var(--success-light); font-weight: bold;"
    >
  </fieldset>
<% end %>
